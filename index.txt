<?php
    require "config.php";
    require 'vendor/autoload.php';

    use iuctoConnect\BaseInitilalize;
    use iuctoConnect\IuctoConnectLib;
    use iuctoConnect\ResultsProcessing;


$init = new BaseInitilalize();

    if ($environment == 'dev') {
        $init->error_initialization();
    }

    $version = "1.2";
    //code
    $connector = new IuctoConnectLib("b1469856e2e628f8ca054e35690156ae");
    $connector->setApiVersion($version);
    $connector->setMethod("GET");
    $connector->prepareConnect();

    //var_dump(json_decode($connector->getCurrency()));
    var_dump($connector->getCurrency()->getResults());
    //var_dump($connector->getCurrency());
    //var_dump(json_decode($connector->getPaymentType()));
    var_dump($connector->getPaymentType()->getResults());
    var_dump($connector->getRoundingType()->getResults(true));

